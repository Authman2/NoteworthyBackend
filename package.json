{
  "name": "noteworthybackend",
  "version": "2.0.0",
  "description": "The backend api for Noteworthy desktop and mobile",
  "main": "index.js",
  "scripts": {
    "test": "nodemon -r dotenv/config src/index.js",
    "start": "node -r dotenv/config src/index.js",
    "clean": "rm -rf .cache dist .cache",
    "deploy": "git add . && git commit -m \"new api version\" && git push && git push heroku master"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "@types/node": "^12.7.2",
    "bcrypt": "^3.0.6",
    "dotenv": "^7.0.0",
    "hapi": "^18.1.0",
    "jsonwebtoken": "^8.5.1",
    "mongoose": "^5.6.11",
    "nodemon": "^1.18.11"
  },
  "engines": {
    "node": "10.x"
  },
  "devDependencies": {
    "typescript": "^3.5.3"
  }
}
